import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    row: {
        display: "flex",
        flexDirection: "row"
    },
    column: {
        display: "flex",
        flexDirection: "column"
    },
    center: {
        alignItems: "center"
    }
})